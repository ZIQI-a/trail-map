# 后端文档索引

本文件是后端 Harness 工程入口。AI 在处理 `backend/` 目录任务时，应先读取 `backend/AGENT.MD`，再按任务需要查阅本索引中的详细文档。

## 必读资料

- 后端 AI 约束入口：`backend/AGENT.MD`
- 项目总需求：`require/全国旅游地图项目.md`
- 数据库草案：`require/数据库设计.md`
- 分阶段计划：`docs/开发规划.md`

## 详细规范

- 后端工程规范：`docs/backend/后端工程规范.md`
- 后端本地配置说明：`docs/backend/后端本地配置说明.md`
- 数据库迁移版本说明：`docs/backend/数据库迁移版本说明.md`
- 阶段 7 用户管理拆分计划：`docs/backend/阶段7-用户管理拆分计划.md`
- 阶段 7.6 个人打卡模块拆分计划：`docs/backend/阶段7.6-个人打卡模块拆分计划.md`
- 地图 API 接入说明：`docs/地图API接入说明.md`

## 当前阶段关注点

- 当前已完成阶段 3 的基础数据接口建设，并进入阶段 7：用户管理与旅行足迹能力迭代。
- 后端已接入 MySQL、MyBatis-Plus、Flyway，并提供城市、景点、标签、POI 校准候选和路线规划接口。
- 用户域基础从 `app_user` 表开始，详细拆分见 `docs/backend/阶段7-用户管理拆分计划.md`。
- 管理端已新增概览聚合、城市、景点基础维护接口：`/api/admin/overview`、`/api/admin/cities`、`/api/admin/spots`，与用户管理接口一样仅对管理员开放。
- 管理端用户、城市、景点列表接口均支持分页；用户列表支持关键词、角色、状态筛选，城市列表支持关键词筛选，景点列表支持城市、关键词、类型、状态筛选。
- 个人打卡模块从后端接口开始推进，定位打卡不作为当前范围，详见 `docs/backend/阶段7.6-个人打卡模块拆分计划.md`。
- 数据库初始化通过 Flyway 迁移脚本执行，不再推荐手动维护散落 SQL；本地运行前需要先查看 `docs/backend/后端本地配置说明.md`。
- 已执行或已提交的 Flyway `V*` 版本脚本禁止直接修改，数据库变更规则见 `docs/backend/数据库迁移版本说明.md`。

## 查阅指引

- 做实体或表结构前：查看 `require/数据库设计.md` 和 `docs/backend/后端工程规范.md`。
- 做 Flyway 迁移前：查看 `docs/backend/数据库迁移版本说明.md`，确认是否必须新增版本。
- 做接口前：先确认接口属于当前开发阶段，再补充或查看接口契约文档。
- 做路线规划前：查看 `docs/地图API接入说明.md`。
- 修改依赖、端口、包结构前：同步检查 `docs/开发规划.md`。
